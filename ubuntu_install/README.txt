***************************************************************************************************
Copy the folder "CalciumImagingAnalyzer" to your desktop; copy the file "install.sh" to your desktop.
to start the software, run the following from the command line while being in your home directory (just open a new command line window, that should do the trick):

$ bash Desktop/install.sh
***************************************************************************************************

for more info see my GitHub repo.
